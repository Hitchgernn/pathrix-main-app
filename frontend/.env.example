# Basemap key necessarily reaches the browser (MapLibre fetches the style URL
# directly) — scope it to basemap access only. The mission key is server-side
# and must never appear here. ARCHITECTURE.md §13.2.
VITE_MAPID_BASEMAP_KEY=
VITE_API_BASE=
VITE_WS_URL=
